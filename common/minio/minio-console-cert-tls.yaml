apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minio-console-tls-cert
  namespace: common-srvs
spec:
  secretName: minio-console-tls-cert
  duration: 8760h
  renewBefore: 720h
  commonName: comsrvs-minio-console
  dnsNames:
    - "*.comsrvs-tls-minio"
    - "*.common-srvs.svc"
    - "*.ds.inesdata.upm"
    - console-storage.ds.inesdata.upm
    - console-storage.ds.inesdata-project.eu
    - comsrvs-minio
    - minio
  issuerRef:
    name: elastic-selfsigned-cluster-issuer
    kind: ClusterIssuer
